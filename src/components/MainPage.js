import React from 'react'
import PropTypes from 'prop-types'
import { connect } from 'react-redux'

export class MainPage extends React.Component {
	render() {
		return (
			<div>
				a
			</div>
		)
	}
}

MainPage.propTypes = {}

export default connect(undefined, undefined)(MainPage)

